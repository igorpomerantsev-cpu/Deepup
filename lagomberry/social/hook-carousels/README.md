# Hook carousels (10 posts, Oct 2026)

Slides for the 10 "Hook Carousels" rows in Lagomberry Ops → Social Posts (rows 122–131).
One folder per `record_id`:

- `ig-N.jpg` — 1080×1350, Instagram. These URLs are in the sheet's `Image` column.
- `tt-N.jpg` — 1080×1920, TikTok photo mode.

Text-only design (colour fields, Archivo Black / Inter / Caveat, gold wordmark #B8956A).
No photos yet — swap in our own bread shots before publishing if wanted.

Re-render: `cd _render && npm i playwright && node render.js ..` (reads `rows.json`).
